#include <stdio.h>
#include <stdio.h>

int main (int argc, char *argv[]) {
    float notas [3];
    char nombres [5][20]; 
    for (int i = 0; i < 5; i++)
    {
        printf("Ingrese el nombre %d: ", i); 
        fflush(stdin); 
        fgets(nombres[i],20,stdin); 

        for (int j = 0; j < 3; j++)
        {
            printf("notas [%d]: ", j); 
            scanf
        }
        
    }
}